name: Deploy Relay

on:
  push:
    branches: [main]
    paths:
      - "services/relay/**"

permissions:
  id-token: write # required for OIDC
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Test relay
        run: npm run test --workspace=relay

      - name: Build relay
        run: npm run build --workspace=relay

      - name: Zip deployment package
        run: cd services/relay/dist && zip -r ../../../function.zip .

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          # Same consolidated deploy role as the HTTP API; its policy covers
          # UpdateFunctionCode on the relay Lambda too.
          role-to-assume: ${{ secrets.AWS_ROLE_ARN_API }}
          aws-region: us-east-1

      - name: Update Lambda function code
        run: |
          aws lambda update-function-code \
            --function-name "${{ vars.RELAY_FUNCTION_NAME }}" \
            --zip-file fileb://function.zip

      - name: Wait for update to complete
        run: |
          aws lambda wait function-updated \
            --function-name "${{ vars.RELAY_FUNCTION_NAME }}"
